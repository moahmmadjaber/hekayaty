enum SharedEnum {
  token,
  id,
  tokenFirebase,
  currentUser,
}

enum MessageType { message, image, file, pdf, m4a, last ,video, call,missedCall}

enum ToastType {
  load,
  success,
  error,
  info,
  toast,
}

enum NotificationType {
  ticketCreate,
  dashBoardDetails,
  mobileAppDetails,
  generalNotification,
  birthDayNotification
}
 enum TaxIncomeStatus {

  request,
  inProgress,
  finished,
  canceled,
  onHold

}

enum AppType {
  facebook,
  youtube,
  telegram,
  callCenter,
  twitter,
  instagram,
  webpage,
  supportTelegram,
  whatsApp,
  mail
}

enum AccountStatementPages { spoStatement, donStatement }

enum UserType { sponsor, donor }

enum IconType { black, normal }

enum LoginScreens { home, phone, qrLogin, qrView, verification }

enum alsaieScreen { home, form, thankYou }

enum boxesScreen { deliveredBoxes, notCollectedBoxes, collectedBoxes }

enum TicketType { spo, boxes, don }

enum LocationType { MAIN, BRANCH, OFFICE, SITE, ATM }

enum CallState {
  ring,
  missCall,
  finished,
  notReachable,
  decline,
  cancel,
  inCall,
  calling
}
 enum CategoryType {
  ticket,
  schoolSupply,
  cafeteria,
  restaurant,
  swimmingPool
}


